/target
**/node_modules
dist/
.DS_Store
*.log

# ── Proprietary / local-only ─────────────────────────────────────────────────
# Large competitor checkouts stay on maintainer machines (never commit).
# Architecture, research, specs, and agents docs under docs/ ARE tracked —
# they are ground truth per AGENTS.md and must not be lost to a local-only ignore.

/competitors/

# Roadmap remains local; agent docs outputs are tracked and stale-checked.
/ROADMAP.md

# CI — local only until we choose to publish pipelines
/.github/
/.claude
