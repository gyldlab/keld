/target
**/node_modules
dist/
.DS_Store
*.log

# ── Proprietary / local-only ─────────────────────────────────────────────────
# Large competitor checkouts stay on maintainer machines (never commit).
# Architecture, specs, engineering, and agents docs under docs/ ARE tracked.
# Numbered research stays local-only — never commit `docs/research/`.
# Sync: `just research-sync` / `just competitors-sync` (see CONTRIBUTING.md).
# Tracked: competitors.lock.toml, .githooks/ — do not ignore those.

/competitors/
/docs/research/

# Public roadmap and generated agent docs are tracked.

# CI workflows, CODEOWNERS, and templates are tracked (KEL-39).
/.claude

# Generated native Cursor registration selects this machine's Python command.
/.cursor/hooks.json
