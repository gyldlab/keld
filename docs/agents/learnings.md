# Learnings log

Evidence log of non-obvious facts. Before implementation/review, query only
the relevant area(s); do not load this file in full. Append a deduped entry in the same
PR when the root self-improvement threshold applies.

Format — one line each, newest last:

```
- YYYY-MM-DD [area] fact. (evidence: path, issue, or command)
```

`[area]` is a base area, optionally suffixed with an OS when the fact is OS-specific
(`wv/macos`, `wv/windows`, `wv/linux`, `bench/macos`). Base areas are the crate short
names (`ipc`, `wv`, `guard`, `native`, `runtime`, `update`, `pack`, `compat`, `core`,
`cli`, `host`) and the cross-cutting areas (`bench`, `bridge`, `build`, `ci`, `deps`,
`docs`, `git`, `process`, `shell`, `tooling`, `ts`). A new value is added to this list in
the same PR that first uses it — an entry whose area is not listed here is the defect,
not the list.

Compaction (maintainers, roughly when this list passes ~40 entries): move stable facts
to the relevant spec, routed playbook, or crate `AGENTS.md`; use root only for a truly
universal invariant that still passes its budget. Delete compacted/superseded/wrong
entries. Until compacted,
agents MUST use bounded area queries rather than a full read.

## Log

- 2026-07-08 [process] Perplexity exports in docs/research/inputs/external/legacy-2026-08/ are raw inputs; canonical notes under docs/research/library/ are the citable corpus — never cite raw inputs directly. (evidence: docs/research/library/compatibility-competitors/00-landscape.md header)
- 2026-07-08 [build] `cargo clippy --workspace --all-targets -- -D warnings` is the CI form; pedantic is already on via workspace lints, do not re-enable it per-crate. (evidence: Cargo.toml [workspace.lints])
- 2026-07-08 [ipc] Wire-size constants (HEADER_LEN=16) are protocol facts tested independently of struct layout — change requires a version bump + review gate. (evidence: crates/keld-ipc/src/frame.rs tests)
- 2026-07-08 [tooling] cargo-deny `unmaintained` accepts `all|workspace|transitive|none`, not `warn`. (evidence: deny.toml, cargo-deny 0.19)
- 2026-07-08 [wv/macos] Phase 1 hello window uses tao+wry as interim scaffolding; replace with direct objc2 bindings per arch/05. (evidence: crates/keld-wv/src/wkwebview/mod.rs)
- 2026-08-08 [wv] wry compiles open_devtools/close_devtools only under cfg(any(debug_assertions, feature="devtools")) — keld-wv pins the `devtools` feature or release builds fail. (evidence: competitors/wry/src/lib.rs `pub fn open_devtools`, crates/keld-wv/Cargo.toml)
- 2026-08-12 [process] IETF RFC 2119 keywords bind agents in AGENTS.md / docs/agents/* only — not architecture prose or Rust comments. (evidence: docs/research/library/agents-tooling/28-post-integration-audit.md, RFC 2119)
- 2026-08-12 [wv/macos] Unit tests must not call `WkWebViewEngine::new` / tao `EventLoop::new` (starts AppKit). Smoke title/HTML/spec instead; `just hello` is the GUI check. (evidence: KEL-26)
- 2026-08-12 [wv/macos] tao 0.35 `EventLoopExtMacOS` already defaults to `NSApplicationActivationPolicyRegular`; do not set it again. (evidence: tao-0.35.3 `platform/macos.rs` trait docs)
- 2026-08-13 [ipc] postcard `from_bytes` ignores trailing bytes; a no-op echo test must assert re-encoded output ≠ input, not expect a codec error. (evidence: crates/keld-ipc/src/echo.rs `echo_roundtrip_copies_fields_not_input_bytes`)
- 2026-08-13 [cli] tao `EventLoop::run` never returns (process exit); hello backends use `run_return` so Drop/reap can run after the last window closes — `keld dev` keeps the supervised Bun echo child alive across the hello window via that path (KEL-30). (evidence: crates/keld-wv/src/wkwebview/mod.rs `run_until_closed`, crates/keld-core/src/hello_session.rs, KEL-30)
- 2026-08-13 [cli] rmcp 3.1.2 accepts `server/discover` only after `initialize`, with namespaced `io.modelcontextprotocol/*` metadata keys; unnamespaced keys return JSON-RPC -32602. (evidence: crates/keld-cli/tests/doctor_mcp.rs `mcp_server_discover_advertises_versions_and_identity`)
- 2026-08-13 [ci] cargo-deny MPL-2.0 is a per-crate pin (`option-ext@0.2.0` via wry→dirs→dirs-sys), not a global allow; packed binaries must offer that crate's source. (evidence: deny.toml, docs/engineering/third-party-licenses.md, KEL-54)
- 2026-08-13 [deps] wry crate is Apache-2.0 OR MIT; GitHub's license API reports Apache-2.0 only. tao is Apache-2.0 only. Do not treat MPL as wry's license. (evidence: crates.io wry 0.55.1 LICENSE.spdx, tao 0.35.3 Cargo.toml.orig)
- 2026-08-13 [ci] gitleaks GitHub Action requires a GITLEAKS_LICENSE on organization repos; pin the OSS CLI tarball with sha256 instead. (evidence: gitleaks-action README org-license note, gyldlab/keld, KEL-39)
- 2026-08-13 [ipc] kipc `decode` uses postcard `take_from_bytes` and rejects leftover bytes; `from_bytes` would ignore them. (evidence: crates/keld-ipc/src/codec.rs)
- 2026-08-13 [cli] `keld hello` extra args must be rejected before `run_hello_window` or the test hangs in tao `EventLoop::run`. (evidence: crates/keld-cli/tests/cli_kel29.rs `keld_hello_unknown_flag_exits_2_without_opening_window`)
- 2026-08-13 [ipc] Darwin/Linux `SO_RCVTIMEO` on a live silent UnixStream is `WouldBlock` (EAGAIN), not always `TimedOut`; map both to `KELD-IPC-006` or a deadline is misclassified as `KELD-IPC-001`. Keep the peer socket alive in the test or you get EOF instead. (evidence: crates/keld-ipc/src/link.rs `read_frame_silent_peer_is_ipc_006`)
- 2026-08-13 [cli] Spec-named verbs that are not live (`build`/`migrate`/`gen`/`ext`) must be `KELD-CLI-045` exit 2, not a bare `unknown command` exit 1 — README agents otherwise have no tracking issue. (evidence: crates/keld-cli/src/verb.rs, KEL-29)
- 2026-08-13 [cli] `keld create --template` must be `KELD-CLI-044` (closed flag set), not `KELD-CLI-020` invalid name `--template`; extra `keld dev` tokens must fail before tao `EventLoop::run`. (evidence: crates/keld-cli/src/flags.rs, KEL-29)
- 2026-08-13 [process] `docs/research/` is local-only; tracking it ships 50+ notes in the PR. `git rm --cached` + gitignore, never `git add docs/research`. (evidence: PR #2, `088efc0`)
- 2026-08-13 [cli] Unix echo sockets in shared `$TMPDIR` are world-connectable; bind inside a unique `0o700` session dir and remove the dir on Drop/shutdown. (evidence: crates/keld-core/src/echo_link.rs, formerly keld-cli)
- 2026-08-13 [ci] dtolnay/rust-toolchain@6c977a6 (2026-08-05) requires `with: toolchain:` even when rust-toolchain.toml exists; omitting it fails every job with 'toolchain' is a required input. (evidence: GitHub CI on PR #2 @ 6ed01b1)
- 2026-08-13 [ci] Windows clippy `-D warnings` fails on unused `std::sync::mpsc` in keld-ipc `echo_link` tests; gate Unix-only test imports with `cfg(unix)`. (evidence: crates/keld-ipc/tests/echo_link.rs, PR #2 windows-latest)
- 2026-08-13 [ci] rustdoc `-D warnings` fails on public docs linking private items (`rustdoc::private-intra-doc-links`); backtick error codes instead of `[`const`]` when the module is private. (evidence: crates/keld-cli/src/mcp/permissions.rs, PR #2 ubuntu rustdoc)
- 2026-08-13 [ci] taiki-e/install-action on windows-latest fails if `%CARGO_HOME%\bin` is missing (tar: Cannot open: No such file). Create the dir before install. (evidence: GHA 31708711822)
- 2026-08-13 [cli] llms-full.txt corpus split on LF-only markers yields empty chunks on Windows CRLF checkout; normalize \r\n in docs_search::corpus(). (evidence: GHA windows-latest docs_search tests on 6cc9749)
- 2026-08-13 [cli] Windows `fs::canonicalize` yields `\\?\…`; doctor JSON must strip that verbatim prefix so MCP (`project_root`) matches CLI (`current_dir`). (evidence: crates/keld-cli/src/doctor.rs `strip_verbatim_prefix`, windows-latest `mcp_doctor_matches_cli_json_over_stdio`)
- 2026-08-13 [process] Hello size/RSS live in `docs/engineering/budget-scoreboard.md` (tracked); `just hello` is debug; do not put numbers only in gitignored `docs/research/` or Linear. (evidence: KEL-26, origin/main `b93ebb6`)
- 2026-08-13 [pack] bun 1.3.14 this-Mac is 63,096,576 B; gzip -9 = 23,548,666 (over the ≤20 MB bun-installer budget before host); zstd -19 = 16,838,595. UDZO/zlib DMG of host+Bun cannot hit architecture 01 §5; zstd can. (evidence: `stat`/`gzip`/`zstd` on `$(command -v bun)`, budget-scoreboard Win conditions)
- 2026-08-13 [process] just 1.58 still lexes `<<'EOF'` bodies inside shebang recipes as justfile syntax (`unknown start of token '-'`); use bash arrays/printf fixtures instead of heredocs in the justfile. (evidence: `just --list` on agents-md heredocs vs array rewrite)
- 2026-08-13 [process] `docs/research/` must be a nested checkout of private `keld-research`; push with `just research-push` (same turn as edits). If it resolves to the Keld toplevel, research-push refuses — never `git add docs/research` from the monorepo root. (evidence: justfile `research-push`, `.agents/research.md`)
- 2026-08-13 [process] Competitor/native hello fixtures live in public `gyldlab/keld-benches` under `{macos|windows|linux}/<framework>/...` (not Keld `docs/`/`competitors/`/`/tmp`-only, not OS-agnostic dumps at benches root); scoreboard rows SHOULD link the OS-qualified path. (evidence: `.agents/research.md`, https://github.com/gyldlab/keld-benches)
- 2026-08-14 [cli] `keld_permissions_explain` must reject unimplemented `channel` before `load_manifest`/`evaluate`; otherwise an in-scope path returns `allow` and drops the channel. (evidence: KEL-58, `crates/keld-cli/src/mcp/permissions.rs`)
- 2026-08-14 [process] keld-benches macOS Electron Forge `package` can exit 0 without writing `out/` (`extract-zip` aborts); weigh official `electron-v*-darwin-arm64.zip` via `ditto`. Neutralino `--macos-bundle` only renames the Mach-O. Electrobun launcher RSS is incomplete until Bun+WebKit helpers are enumerated. (evidence: gyldlab/keld-benches@0308d55 macos/*/hello, docs/engineering/budget-scoreboard.md)
- 2026-08-14 [wv/macos] wry 0.56 still auto-grants camera/mic when `with_permission_handler` is omitted; the handler is `Fn(PermissionKind)` with no origin, so v0 evaluates requested resource `*`. (evidence: wry-0.56.1 `wry_web_view_ui_delegate.rs`, KEL-59)
- 2026-08-14 [ipc] kipc v2 HELLO carries a 32-byte session token from `KELD_APP_LINK` (`<endpoint>#<64 hex>`); empty/foreign HELLO is `KELD-IPC-007`. Client writes first; server must not write the secret until the peer proves possession or Windows loopback TCP leaks it. Named-pipe DACL is not this slice. (evidence: KEL-60, crates/keld-ipc/src/link.rs `handshake_server`, crates/keld-ipc/src/token.rs, crates/keld-cli/src/echo_link.rs)
- 2026-08-14 [guard] v0 `evaluate` must deny non-`AppProcess` *before* grant lookup; otherwise an empty-manifest webview becomes `NotGranted` and the fix tells you to add `/app` scopes (collapses the three-tier model). (evidence: KEL-61, `crates/keld-guard/src/lib.rs` `non_app_principal_is_denied_before_grant_lookup`)
- 2026-08-14 [bench] Timing first paint in an embedded webview: `document.title` never reaches the native window caption (the framework owns it, not the document) and `fetch()` is CORS-blocked on the opaque `with_html`/`NavigateToString` origin — use an `<img>` beacon inside a double `requestAnimationFrame` against one local `HttpListener` so all arms share a clock; titled-`HWND` time is presentation policy, not paint (real Keld-vs-Tauri gap 1.8x, not the 6.6x that column implied). (evidence: docs/engineering/budget-scoreboard.md "Time to first paint (2026-08-14)", gyldlab/keld-benches MEASUREMENTS.md)
- 2026-08-14 [wv/windows] wry sets WebView2 controller bounds only from its WM_SIZE subclass hook, so a webview never resized after attach stays 0x0 and Chromium does not composite it (~640 ms first-paint stall on hello); call `webview.set_bounds(window.inner_size())` once after build. Also: PowerShell `Start-Process` costs 450-700 ms before child main() — measure spawn offset via a wall clock at child entry, or use .NET `Process.Start` (~11 ms). (evidence: KEL-62, crates/keld-wv/src/webview2/mod.rs, keld-benches windows/bench)
- 2026-08-15 [wv/windows] wry 0.56.1 `attach_ipc_handler` blocks the UI thread 96-109 ms injecting `window.ipc` via a synchronous `AddScriptToExecuteOnDocumentCreated` wait even when `ipc_handler` is `None`; WebView2 environment creation is only 3-6 ms (runtime resolution — the browser process launches at controller creation, which is the 313-506 ms floor). Any future Keld script injection must use the async completion-handler form. (evidence: KEL-65 attribution, competitors/wry/src/webview2/mod.rs:965)
- 2026-08-15 [bridge] `Bun.connect({unix: "/path"})` (client-side Unix domain socket) is real and documented in `bun-types` but absent from bun.sh's own rendered docs pages and Context7's indexed `tcp.mdx` (both only show `hostname`/`port`); ground truth is `packages/bun-types/bun.d.ts` on `oven-sh/bun` (`UnixSocketOptions`/`TCPSocketConnectOptions`, both accepted by an overloaded `connect()`). `socket.write()` returns bytes written, `-1` on a closed socket, and can be a **partial** write under backpressure — must loop with the `drain` handler, not assume one `write()` call sends everything. (evidence: KEL-30, crates/keld-cli/templates/hello/src/kipc.ts, raw.githubusercontent.com/oven-sh/bun/main/packages/bun-types/bun.d.ts)
- 2026-08-15 [cli] `keld dev`'s window step was gated `#[cfg(target_os = "macos")]` in `dev.rs` even though `keld_core::run_hello_window_html` (the function it would have called) was already cross-platform and dispatches per-OS internally (`keld_wv::hello::run`) — pure staleness left over from before the Windows WebView2 backend went live (KEL-27, then KEL-65). Before adding a platform cfg gate around a call, check whether the callee already handles the platform split; a redundant outer gate silently rots when a new backend lands. (evidence: KEL-29, `crates/keld-cli/src/dev.rs` `open_dev_window`, `crates/keld-core/src/hello.rs`)
- 2026-08-16 [wv/linux] wry 0.56.1's Linux backend is GTK3 + WebKit2GTK 4.1 (`gtk` 0.18, `webkit2gtk` 2.0.2), not GTK4/`webkitgtk-6.0` — architecture 05's "webkit6/gtk4" phrasing describes the *destination* direct-bindings rewrite, not what wry-interim actually links. apt packages to build it: `libgtk-3-dev libwebkit2gtk-4.1-dev libjavascriptcoregtk-4.1-dev libsoup-3.0-dev pkg-config build-essential`. KEL-132 removed live environment mutation: process entry passes explicit `argv`/`envp` to exact-self `execve`, and engine construction fails closed if unprepared. clippy `doc_markdown` flags bare "WebKitGTK" in doc comments; backtick it. (evidence: KEL-28, KEL-132, crates/keld-wv/Cargo.toml, crates/keld-wv/src/webkitgtk/mod.rs)
- 2026-08-16 [process] WSL reporting `WSLg version: 1.0.66` via `wsl --version` does not mean a display is reachable: `/tmp/.X11-unix` and `/mnt/wslg/.X11-unix` can stay empty and `$DISPLAY`/`$WAYLAND_DISPLAY` unset even after `wsl --shutdown` + restart, as the default non-root user (per `/etc/wsl.conf` `[user] default=`). `gtk::init()` then fails with "Failed to initialize GTK" — a clean, unambiguous signal the sandbox has no compositor, not a code defect to chase. (evidence: KEL-28, `wsl --version`, tao-0.35.3 `platform_impl/linux/event_loop.rs` panic text)
- 2026-08-16 [process] TencentDB Agent Memory v2.0.1-beta.2's quick-start publishes four ports on all interfaces while leaving the Memory Core Bearer gate empty for Proxy compatibility; a Keld pilot must stay optional, bind loopback, and use immutable source/image pins. (evidence: KEL-67, upstream `deploy/global-images/start-memory-core.sh`, `start-memory-hub.sh`, `start-proxy.sh`)
- 2026-08-16 [process] Current Codex uses a named `$CODEX_HOME/<name>.config.toml` overlay (default `~/.codex/<name>.config.toml`) only when `--profile <name>` is selected; do not write new setup docs with legacy `[profiles.*]`. (evidence: local `codex-cli 0.148.0-alpha.9 --help`; https://learn.chatgpt.com/docs/config-file/config-advanced#profiles)
- 2026-08-17 [bench] A key NSWindow is not proof that its WKWebView has document focus: stock Neutralino 6.9.0 makes the window key but never makes the WebView first responder, so KEL-64 must fail closed instead of fabricating a click or accepting an unfocused beacon. (evidence: neutralinojs v6.9.0 `lib/webview/webview.h`, `/tmp/keld-kel64-neutralino-direct-url-one-20260817.json`)
- 2026-08-17 [bench] For Neutralino v6.9.0 on macOS, calling `NSWindow makeFirstResponder:` for the runtime `WKWebView` after the documented `window.focus()` path's bring-to-front/restore branch is the minimal focused-document fix: a clean-source build passed 11/11 strict KEL-64 beacons; startup/delegate reassertions and `setInitialFirstResponder:` were unnecessary. (evidence: gyldlab/keld-benches@4666d0a, `/tmp/keld-kel64-neutralino-focus-onecall-20260817.json`)
- 2026-08-17 [bench] KEL-64 supplies `KELD_BENCH_TOKEN` to every arm; an opt-in fixture trace must disable itself when its trace-path variable is absent, not when the token is absent, or normal benchmark arms exit before launch ownership can be captured. (evidence: gyldlab/keld-benches@258756c, `/tmp/keld-startup-untraced-one-20260817.json`)
- 2026-08-17 [bench] wry `PageLoadEvent::Finished` is navigation completion, not composited first paint, and a product clock started after spawn cannot support architecture 01 §5; KEL-64 scores are the external double-rAF beacon on untraced arms only. (evidence: gyldlab/keld#10, keld-benches@aae2e12 `macos/keld/hello/kel64-startup-attribution.json`)
- 2026-08-17 [runtime] Windows `cmd /C "step1; step2"` does not treat `;` as a statement separator — cmd only splits on `&`; a `;`-joined test script silently runs as one `echo` argument and never reaches `exit N`, so a crash-loop test built from `sh -c`/`cmd /C` pairs needs an OS-aware join (`; ` on Unix, ` & ` on Windows), not one script string for both. (evidence: KEL-70, `crates/keld-runtime/src/lib.rs` `joined_steps`, first `crash_loop_breaker_trips_after_max_crashes` run reporting `Stopped` instead of `CrashLoop` on Windows)
- 2026-08-17 [cli] `crates/keld-cli/tests/error_registry.rs` `SCAN_REL` only scans `keld-ipc`/`keld-wv`/`keld-cli`/`keld-guard`/`tools` by default; a new crate emitting its first `KELD-*` code (e.g. `keld-runtime`) needs an explicit `SCAN_REL` entry or the registry test silently never checks it. (evidence: KEL-70, `crates/keld-cli/tests/error_registry.rs`, `docs/engineering/keld-error-codes.md`)
- 2026-08-17 [wv/linux] `WebViewBuilderExtUnix::build_gtk` must be given tao's `WindowExtUnix::default_vbox()`, not `gtk_window()` — a `GtkApplicationWindow` is a `GtkBin` (one child max) and tao already fills that slot with its own vertical `gtk::Box`. `gtk_window()` compiles, spawns a real (titled, xdotool-visible) window, and even runs without panicking, but GTK logs "can only contain one widget at a time" and the webview never actually attaches — a real, silent functional bug, not just a warning to suppress. Every real wry example (`examples/simple.rs` etc.) uses `default_vbox()`; the `gtk_window()` form only appears in wry's simplified top-of-crate-doc snippet, which is for `build(&window)` (X11-only), not `build_gtk`. (evidence: KEL-28 follow-up, `competitors/tao/src/platform/unix.rs`, `competitors/wry/examples/simple.rs`, `crates/keld-wv/src/webkitgtk/mod.rs`)
- 2026-08-17 [ci] A bash `trap '... || true' EXIT` clobbers `$?` with the trap's own last command status — a script that hit `exit 1` reports success to its caller. Capture the real code first (`trap 'ec=$?; cleanup; exit "$ec"' EXIT`), not as an afterthought. Also: nesting `\$?` inside multiple layers of `bash -c "..."`/`wsl ... -c "..."` double-quoted strings lets an *outer* shell expand it before the inner script ever runs, silently baking in a stale value — write multi-layer test harness scripts to files instead of nesting quoted `-c` strings. (evidence: `.github/workflows/ci.yml` `linux-gui-smoke` job, local WSL dry-run debugging)
- 2026-08-17 [guard] `keld-guard::evaluate` already denies any `..` path segment unconditionally (checked before scope matching) — a privileged handler using `dispatch_privileged` gets `..` traversal protection for free and must not re-implement it. Trap for the *test*, not the guard: `<granted-dir>/../<name>` always resolves to the shared temp root regardless of `<granted-dir>`'s own unique suffix, so a fixed escaping filename (`../outside.txt`) can collide with a crashed prior run or a parallel test on the same real path and produce a false failure — give the escaping filename itself a unique suffix too. (evidence: KEL-71, `crates/keld-native/src/fs.rs` `dotdot_segment_is_denied_even_inside_a_granted_scope`)
- 2026-08-17 [ipc] A second `echo_call` on a live app-link sends HELLO again: the server fails with `KELD-IPC-005`, the client observes `KELD-IPC-001` because the peer closed — not 005 on both sides. Use `echo_invoke` for further CALLs. (evidence: KEL-30, `crates/keld-ipc/tests/echo_link.rs` `second_echo_call_on_same_stream_client_001_server_005`)
- 2026-08-17 [bench] `kel64-startup-attribution.json` beacon 352.211 ms is the traced-arm diagnostic (`evidenceKind` `spec-section-7-paired-diagnostic-not-a-published-score`); keld-benches has no committed untraced `--publish` JSON after AC4. (evidence: keld-benches@aae2e12 `macos/keld/hello/kel64-startup-attribution.json`, `MEASUREMENTS.md`)
- 2026-08-18 [ts] Bun 1.3.14 `bunfig.toml` `[alias]` does not remap runtime `import "electron"` — `import.meta.resolve("electron")` still returns `~/.bun/install/cache/electron`; `tsconfig.json` `compilerOptions.paths` does remap it. (evidence: KEL-72, `packages/@keld/electron/fixtures`)
- 2026-08-18 [compat] Integration stdout `has()` on an accumulator misses lines still queued on the reader `rx`; drain `try_recv` and assert WAITING-without-READY *before* `signal_ready` or a stub `whenReady` can pass. (evidence: KEL-72 finding 3, `crates/keld-compat/tests/electron_lifecycle.rs`)
- 2026-08-18 [compat] A Bun fixture that `await`s `app.quit()` but keeps a kipc read loop open will not exit; the host `wait()` hangs. Close the socket and `process.exit(0)` after the Quit reply. (evidence: KEL-72, `crates/keld-compat/tests/electron_lifecycle.rs`)
- 2026-08-18 [ts] Bun 1.3.14 delivers `unhandledRejection` on a macrotask; two `await Promise.resolve()` ticks still report COUNT=0. Await `setImmediate` before reading the count. (evidence: KEL-72 `packages/@keld/electron/fixtures/app_ready.ts`)
- 2026-08-18 [core] Win32 `TcpStream::shutdown` on a cloned handle does not wake a blocking `read` on another thread (rust-lang/rust#121594); lifecycle Drop must poll `SO_RCVTIMEO` + stop flag. Windows `write_timeout()` on a clone is not an oracle for `SO_SNDTIMEO`. (evidence: PR #27 windows-latest, `crates/keld-core/src/lifecycle.rs`)
- 2026-08-18 [ts] `bun test` shares a process-wide module cache across files; stubbing `LifecycleLink.connect` in one `*.test.ts` leaks into `link.test.ts`. Spawn a fixture child instead. (evidence: KEL-72 `packages/@keld/electron/src/app.test.ts`, bun 1.3.14)
- 2026-08-18 [ipc] `SO_RCVTIMEO` is per-`recv`; `read_frame_interruptible` retrying poll timeouts after a partial frame never hits `KELD-IPC-006` unless a wall-clock stall deadline starts on the first byte of that frame. Non-blocking is unsupported (`WouldBlock` is poll expiry). (evidence: PR #27 CodeRabbit, `crates/keld-ipc/src/link.rs` `interruptible_read_stalled_mid_header_is_ipc_006`)
- 2026-08-18 [compat] writeOneFrame must bound drain.wait() by remaining wall-clock from the first write; restarting withIoDeadline per wait lets a draining peer never hit KELD-IPC-006. (evidence: PR #27 CodeRabbit, packages/@keld/electron/src/link.ts)
- 2026-08-18 [wv] Camera/mic must not evaluate as AppProcess; a minted WebviewId is the principal, and missing identity is KELD-GUARD007 fail-closed. (evidence: CodeRabbit on PR #27, KEL-73, crates/keld-wv/src/media.rs)
- 2026-08-19 [ipc] Unlinking a Unix bootstrap socket path after authentication is not the same as closing the listener fd; one-shot admission needs host-owned listener state (`Option<UnixListener>`) so shutdown/deadline/bind close the fd and remove the locator together. (evidence: KEL-75, `crates/keld-ipc/src/bootstrap.rs`)
- 2026-08-19 [ipc] A non-blocking Unix listener can admit a stream that behaves non-blocking on macOS; normalize the accepted app-link stream back to blocking before applying `APP_LINK_IO_DEADLINE`, otherwise the session loop observes immediate `KELD-IPC-006` after a quiet-but-live reply boundary. (evidence: KEL-75, `crates/keld-ipc/src/bootstrap.rs`, `crates/keld-cli/src/echo_link.rs` `roundtrip_over_loopback_copies_fields`)
- 2026-08-19 [runtime] macOS Unix-domain socket fixture paths must reserve bytes for the socket file itself; `/var/folders/...` from `std::env::temp_dir()` can exceed `SUN_LEN`, so control sockets should use a short owner-only `/tmp/<short-id>` directory. (evidence: KEL-75, `crates/keld-runtime/src/primary.rs` `PrimaryFixture::new`)
- 2026-08-19 [docs] Any new `KELD-*` code emitted from a workspace tool must also be added to `docs/engineering/keld-error-codes.md`; the CLI registry test scans `tools/` and fails even when the tool itself compiles. (evidence: `tools/mermaid_docs.rs`, `tools/mermaid_render_check.sh`, `crates/keld-cli/tests/error_registry.rs`)
- 2026-08-19 [compat] `score` must re-check empty/duplicate cells, result/waiver pairing, and evidence URIs; parse-only gates do not bind hand-built structs, so empty `cells` was `complete` `0/0`, one Pass in a duplicated cell list was 2/2, and Pass+unexpired-waiver was 100%. (evidence: KEL-74, PR #34, `crates/keld-compat/src/evidence.rs`)
- 2026-08-19 [compat] A 40-hex path segment does not make `/blob/<branch>/` immutable; reject `blob`/`tree`/`raw` (and GitHub raw CDN) refs that are not themselves a git object id. (evidence: KEL-74, PR #34, `https://github.com/gyldlab/keld/blob/main/<40-hex>` parsed at 8f8181d)
- 2026-08-19 [compat] Omitting `unweighted_percent` is not enough: product `complete` is a second publish channel, and `Scoreboard` public fields let a caller mint 100% without `score()`. (evidence: KEL-74, PR #34, `product_panel_omits_percent_for_uncommitted_corpus`)
- 2026-08-19 [compat] `Ipv4Addr` rejects a trailing FQDN dot and `to_ipv4_mapped` ignores deprecated `::a.b.c.d` / `::ffff:0:a.b.c.d`, so RFC1918/169.254 still parsed as public https evidence until the host is trimmed and those embeddings share the v4 check. (evidence: PR #34, `https://10.0.0.1.` and `https://[::10.0.0.1]/` at 1a334f1)
- 2026-08-19 [compat] After the FQDN-dot / mapped / compatible / translated close, NAT64 `64:ff9b::/96`, 6to4 `2002::/16`, and CGNAT `100.64.0.0/10` still counted as public https evidence; abbreviated IPv4 and DNS-to-private are T1 residuals (literal host, no DNS). (evidence: PR #34, `64:ff9b::10.0.0.1`, `2002:a00:1::1`, `100.64.0.1`)
- 2026-08-19 [compat] `Ipv4Addr::is_shared` is still unstable (`feature(ip)` / E0658 on 1.97.1); CGNAT `100.64.0.0/10` must be octet-checked. (evidence: PR #34, `cargo clippy -p keld-compat`)
- 2026-08-19 [compat] GitHub raw CDN live-ref checks must trim a trailing FQDN dot; `raw.githubusercontent.com./…/main/<40-hex>` bypassed the host equality and counted as a pin. Unbracketed `host:abc` was kept as the hostname when the port was non-numeric. (evidence: PR #34, `rejects_trailing_dot_github_raw_cdn_live_ref`, `rejects_malformed_https_ports`)
- 2026-08-19 [ci] Required GitHub checks must use an always-created workflow plus job-level routing: workflow-level path filters leave required checks pending. Derive impacted Rust and host closures from `cargo metadata`; if metadata is unavailable, fail the router rather than emit an empty green selection. (evidence: KEL-81, `tools/ci_changes.sh`, GitHub Actions documentation)
- 2026-08-19 [ci] `cargo nextest run -p <selected-package>` exits 4 for a valid zero-test package while the workspace suite does not; dynamic package routing must set `--no-tests=pass` and retain consumer-package tests, not drop empty packages or weaken test failures. (evidence: KEL-81 macOS CI job 95978373698, `cargo nextest run --help`)
- 2026-08-19 [ci] Parallel Ubuntu `apt-get update` on clippy/MSRV plus GUI smoke hangs on Azure mirrors (~30–45 min cancel) while the same WebKitGTK packages succeed on GUI smoke alone in ~2 min; MSRV is a rustc gate (macos), and Ubuntu GTK apt belongs only on keld-wv/core/host ownership or GUI smoke. (evidence: KEL-81 PR #36 jobs 95979085798/95979085845 vs linux-gui-smoke)
- 2026-08-19 [ci] `jobs.<job_id>.if` cannot use `matrix` (contexts: github, needs, vars, inputs); `matrix.os` there invalidates the workflow so rustc and gitleaks never start. Keep OS guards on steps. (evidence: GitHub context-availability table, PR #36 run 32244879790)
- 2026-08-19 [runtime] `CLONE_NEWNS` initializes a copy of the parent's mount list; Linux strict still needs an explicit host-path deny plus role-private allow tests, and Landlock is not that deny. (evidence: KEL-78, `mount_namespaces(7)` man-pages 6.18, `docs/specs/kel78-strict-profile-sandbox.md`)
- 2026-08-19 [runtime] KEL-78 `admit` cannot return Strict from protocol/cleanup/limit rows or a binary digest alone; the archive needs a complete OS-containment catalog and a matching token/profile digest. (evidence: KEL-78, `docs/specs/kel78-strict-profile-sandbox.md`)
- 2026-08-19 [runtime] KEL-78 forbids inherit-from-host and child-ambient sockets from a guard grant; leaving Strict is admission failure, not an unsandboxed restart. (evidence: KEL-78, `docs/specs/kel78-strict-profile-sandbox.md`)
- 2026-08-19 [shell] In zsh, lowercase `path` is tied to `PATH`; using `path` as a loop variable can erase command lookup mid-script. Use names like `changed_file` in hygiene/review loops. (evidence: KEL-75 review hygiene command, `docs/agents/learnings.md`)
- 2026-08-19 [ipc] A Unix bootstrap cancellation wake socket is only a nudge; on macOS `shutdown()` on the freshly connected wake stream can race the accepting side and return `NotConnected`. Treat that as successful cancellation, not a real listener failure. (evidence: KEL-75, `crates/keld-ipc/src/bootstrap.rs` `shutdown_unblocks_accept_and_removes_owner_only_directory`)
- 2026-08-19 [runtime] Latch bootstrap `admission_done` on deadline/failure/disconnect; otherwise a later `revoke` can overwrite the real admission error with `Disconnected`. (evidence: KEL-75 T1b review, `crates/keld-runtime/src/unix_role.rs`)
- 2026-08-19 [runtime] Bun 1.3.14 `subprocess.kill()` called after the child's `'exit'` returns `true` and sets `subprocess.killed = true` even though a raw `process.kill(pid, 0)` at the same instant throws `ESRCH`; Node v26.7.0 returns `false`/`false`. A supervisor that branches on `kill()` to decide whether to escalate to `SIGKILL` reads success from a signal that was never delivered. Divergences in this family are parent-attributed — always probe the parent x child runtime matrix before blaming the child. (evidence: KEL-77, `cargo nextest run -p keld-runtime bun_kill_after_exit_is_the_pinned_defect`, crates/keld-runtime/fixtures/child-process/)
- 2026-08-19 [runtime] Dropping path-NUL-length framing from the KEL-77 corpus digest still passes a one-byte fixture flip; `("ab","c")` vs `("a","bc")` is the test that fails (concatenated SHA-256 of `abc`). (evidence: KEL-77 T2 negative control, `corpus_digest_rename_plus_content_shuffle_does_not_collide`)
- 2026-08-19 [guard] KEL-78 T1 `admit` is in `keld-guard` (`KELD-GUARD008`–`015`); the spec sketch's `KELD-RUNTIME-0xx` names the caller, not the emitting crate. Live `bind`/`connect` success or `ConnectionRefused` is not an OS-deny pass. (evidence: KEL-78, `crates/keld-guard/src/admit.rs`)
- 2026-08-19 [guard] `ProofLayerMismatch` (`KELD-GUARD015`) Display must include the recorded oracle; `recorded_layer == OsContainment` makes expected and recorded layer ids identical when a JS-shim or host-protocol oracle is written into an OS slot. (evidence: KEL-78 T1 PR #41, `crates/keld-guard/src/admit.rs`)
- 2026-08-20 [process] GitHub YAML issue forms are issues-only; PR templates are Markdown. Multiple PR templates have no chooser (query param only). HTML comments persist in `gh pr view` unless stripped. Feature branches are `agent/kel-<n>-<slug>` from `origin/main`, not Linear's `gyldlab/kel-…`. (evidence: docs.github.com issue-forms + PR-template pages; `gh pr view` #34/#41; KEL-84)
- 2026-08-20 [process] Deleting only the PR template's top HTML comment still leaves per-heading hints in `gh pr view`; strip every `<!-- -->` before submit. (evidence: KEL-84, `.github/PULL_REQUEST_TEMPLATE.md`)
- 2026-08-21 [git] unused extra worktrees MUST be removed before creating another; open-PR and dirty trees stay. (evidence: disk; user 2026-08-21)
- 2026-08-21 [process] Branch + Linear handoff (`## Branch handoff` + merge intent) is binding in `.agents/coordination.md`, not only Prompt Tracker pastes; multi-device merge intent is Linear. (evidence: alignment audit; `0monish/prompt-tracker` `548e7db`)
- 2026-08-21 [runtime] Bun 1.4 fixed `subprocess.kill()` after `'exit'`; floating `oven-sh/setup-bun` `latest` would fail the former KEL-77 pinned-defect test — pin CI to exact `1.4.0`. (evidence: oven-sh/bun `fc1b448`, KEL-77, `.github/workflows/ci.yml`)
- 2026-08-21 [process] Local analysis clones under gitignored `competitors/` (bun, wry, tao) are invisible to agents unless listed in `competitors.lock.toml`; `just competitors-sync` only fetches lock entries. (evidence: competitors.lock.toml, tools/competitors_sync.rs, KEL-68)
- 2026-08-21 [ci] Adding `keld-runtime` under `keld-core` puts runtime in the host dependency closure (`gui=true` for runtime-only paths); host-dirs must walk only normal cargo deps (`kind == null`), not `dev`/`build`, or runtime's test-only `keld-compat` edge falsely enables GUI smoke. (evidence: tools/ci_changes.sh `host_dependency_dirs`, KEL-30, PR #48)
- 2026-08-21 [guard] Crate/lib + arch/03 must not claim every privileged op / live dev-permissive+recorder until those call sites exist; v0 live evaluate is media (empty default manifest) + host fs via dispatch_privileged; keld-core depends but does not call evaluate. (evidence: KEL-93, crates/keld-guard/src/lib.rs, docs/architecture/03-security.md §3)
- 2026-08-21 [host] KEL-30 “host-owned” hello echo is crate ownership (`keld-core::HostOwnedHelloSession`); `keld dev` still runs that session in the CLI process — Unique #1 needs no-flag `keld-host` process ownership, gated on approved research-83 + kel96 spec. (evidence: KEL-96, research `83-host-vs-cli-ownership.md` @ `5118d6b`, `crates/keld-cli/src/dev.rs`)
- 2026-08-22 [process] Addressed CodeRabbit GitHub review threads stay open until an agent calls `resolveReviewThread`; follow-up PR fixes must be resolved on the original PR with a merge-SHA comment. (evidence: PR #54/#55 thread cleanup, `.agents/skills/autofix/github.md` § Resolve review threads)
- 2026-08-22 [process] Do not merge a PR while CodeRabbit review threads on it remain unresolved — land fixes and resolve threads first, or resolve on the original PR after a follow-up merge. (evidence: PR #58 merged at `1609dbd` with open threads, KEL-75 follow-up)
- 2026-08-22 [wv] `startup` is WKWebView-only; gate `mod startup` to `#[cfg(target_os = "macos")]` or Linux/Windows CI promotes `dead_code` to errors. (evidence: KEL-62, CodeRabbit #10, `crates/keld-wv/src/lib.rs`)
- 2026-08-22 [process] `include_str!` / source-grep wiring tests pass when helper text exists but `create` never registers the callback; invoke the handler and assert observable state instead. (evidence: KEL-62, CodeRabbit #10, `crates/keld-wv/src/wkwebview/mod.rs` `startup_tests`)
- 2026-08-22 [wv] `KELD_STARTUP_TRACE=1` must emit `nav_finished=never` when `PageLoadEvent::Finished` never fires, or hung navigations leave no stderr evidence. (evidence: KEL-62, CodeRabbit #10, `crates/keld-wv/src/startup.rs` `should_emit_nav_never_finished`)
- 2026-08-23 [ci] Git Bash reports the repo root as `D:/...` while cargo.exe metadata uses JSON `D:\\...`; normalize only Windows drive/UNC metadata filesystem fields once at ingestion, because a backslash inside an absolute Unix path is a valid filename byte and a root-relative Windows path cannot match a drive-qualified repo root. Exercise native shapes per OS plus native-jq CRLF in the portable fixture. (evidence: KEL-149, CodeRabbit #127, `tools/ci_changes.sh`, `tools/ci_changes_test.sh`)
- 2026-08-23 [runtime] A host lifecycle drain must both revoke virtual-port state and retain the `Revoked` event for the public role feed; exposing the mutable port table lets route operations bypass that single synchronization boundary. (evidence: KEL-75 CodeRabbit CLI review, `crates/keld-runtime/src/registry.rs` `role_port_operations_sync_revocation_and_preserve_lifecycle_event`)
- 2026-08-23 [runtime] When `RoleRegistry` owns lifecycle synchronization, shutdown tests must await supervisor completion then assert through its `try_recv_*_event` feed; reading the supervisor queue directly cannot prove the public buffered contract. (evidence: KEL-75 PR #59 fresh CodeRabbit review)
- 2026-08-23 [runtime] A role test probe emitted at `Provisioned` cannot prove `Spawned` is queued; emit it after `Spawned` so a registry FIFO assertion awaits the producer's real milestone instead of polling for timing. (evidence: KEL-75 PR #59 CodeRabbit follow-up)
- 2026-08-23 [process] A CI lane on the named OS proves its declared automation contract, not a visible-window, device, installer, sandbox, or other real-product acceptance unless the issue/spec explicitly defines that fixture as the evidence. (evidence: KEL-100 Windows proof vs macOS/Linux CI, KEL-108)
- 2026-08-23 [ipc] A persistent echo session must replace the post-HELLO reader deadline with `APP_LINK_READER_POLL` plus `read_frame_interruptible`: retaining the five-second `SO_RCVTIMEO` turns a quiet host window phase into `KELD-IPC-006`; writer and partial-frame stall limits remain five seconds. (evidence: KEL-100, crates/keld-ipc/src/session.rs)
- 2026-08-23 [cli] `run_dev` writes one captured Bun stdout snapshot before opening the GUI and does not live-forward later child logs; a product proof of a later Bun action needs an ordered independent witness, so absent terminal stdout is not by itself an IPC failure. (evidence: KEL-100 Windows product proof, crates/keld-cli/src/dev.rs `run_dev`)
- 2026-08-23 [process] In the Prompt Tracker checkout (`0monish/prompt-tracker`, typically `keld-agent-prompts`), new categories must be added to `src/data/schema.ts` (`PROMPT_CATEGORIES`), `src/data/taxonomy.ts` (`CATEGORY_LABELS`), and `scripts/local-catalog.ts`, or typecheck/seed fail. (evidence: KEL-109, `NEW/audit/01`)
- 2026-08-23 [ipc] `PipeAccessRights::ReadWrite` is `0x0002_019B`, not final pipe ACE `0x0012_019B`; an explicit `SYNCHRONIZE` OR and descriptor read-back keep the intended access mask testable. (evidence: KEL-101, CodeRabbit PR #69, local .NET `PipeAccessRights` reflection)
- 2026-08-24 [docs] Crate `//!` headers and Cargo `description` fields drift toward destination wording faster than the arch docs: audit 191 found 7/11 crates describing TARGET scope as identity while arch 01 §3 was mostly right. Treat both as current-state claims in KEL-91/92/93-class reviews. (evidence: research 191 §7, KEL-110)
- 2026-08-24 [process] A recompute-from-raw publication check must bind historical inputs to immutable commits, blobs, and hashes; reading mutable raw plus mutable tables can pass self-consistently after both are changed. (evidence: gyldlab/keld-benches PR #10 `Check-PublishedMeasurements.ps1` negative control)
- 2026-08-24 [git] The nested `docs/research` checkout can be left on another device's feature branch; `git push origin main` from there pushes the stale local `main` ref and exits 0 while HEAD's commits stay unpublished. Run `git -C docs/research branch --show-current` before committing and verify the landing with `git -C docs/research ls-tree --name-only origin/main | grep <note-id>` — a push that printed no error is not evidence. (evidence: 2026-08-24 audit session; notes 192/196/212/187/188 and two 195 deltas sat on `research/194-bench-stat-repair` until re-published)
- 2026-08-24 [ipc] A per-broker `FrameKind::Err` payload drifts within one repo: before KEL-102 each broker encoded `err.to_string()`, and the `keld-ipc` marker-session fixture shipped an `io::Error` string with no `KELD-*` code at all, so a peer matching on `KELD-` got nothing. One `CallError { code, message }` written by `keld_ipc::write_call_error` gives the deny payload a single owner; the two shapes are mutually undecodable: a bare `String` consumes the whole payload leaving no second varint (postcard), and a `CallError` leaves trailing bytes that `keld_ipc::codec::decode` rejects — plain `postcard::from_bytes` would NOT, it would silently return `code` as the whole string. So the migration fails loudly in both directions: `KELD-IPC-003` in Rust, surfaced as `KELD-IPC-005` by `@keld/electron`. (evidence: crates/keld-ipc/src/call_error.rs, crates/keld-ipc/tests/guard_dispatch.rs)
- 2026-08-24 [runtime] `keld dev` surfacing an app death only via the crash-loop breaker is timing-dependent, not a fix: `KELD-RUNTIME-002` needs 3 crashes inside a *sliding* 30s window, so one death — or a slower cadence — leaves a clean `Stopped` outcome over a dead app. Read durable crash state instead. Verified live on macOS: after `kill -9` the restarted generations could not re-enter the one-session echo listener (`Bun.connect` ENOENT) and crashed fast enough that the breaker did trip — so a test built on that trip passes over the real hole. (evidence: KEL-105 live run, `keld dev` exit 1 with `KELD-CORE-033`; `crates/keld-runtime/src/lib.rs` `RestartPolicy`/`crash_times.retain`)
- 2026-08-24 [runtime] "Any crash fails the run" is not a valid reading of a dead app: KEL-70 AC1/AC3 requires a crash the supervisor recovered from to stay a success, so the discriminator is the crash count at the ready marker, not the crash itself. (evidence: KEL-105, `crates/keld-cli/tests/cli_kel29.rs` `run_dev_echo_recovers_from_one_real_bun_crash`)
- 2026-08-24 [runtime] BSD `accept()` gives the accepted socket the listener's file status flags, so on macOS a stream accepted from a `set_nonblocking(true)` listener arrives with `O_NONBLOCK` set and `SO_RCVTIMEO` is silently inert — reads return `EAGAIN`/`WouldBlock` immediately instead of waiting for the configured deadline. Linux's `accept4` does not inherit, so the same code is only flaky on macOS. Clear it explicitly on the accepted stream; a read deadline you never verified is not a deadline. (evidence: KEL-113, `crates/keld-runtime/src/unix_role_fixture.rs` `ControlPeer::new`, CI `clippy + test (macos-latest)` run 32765297839)
- 2026-08-24 [runtime] `keld-runtime` publishes stdout and `SupervisorEvent::Exited` *before* `record_crash`, so a host that samples the crash *count* when it first notices a ready marker can already see a post-ready death and fold it into its "recovered" baseline — reporting success over a dead app. Counting crashes cannot order them against app output; the ledger records stdout length at each crash and the host compares the marker's offset. (evidence: KEL-105 re-open, `crates/keld-runtime/src/lib.rs:725-756`, `crates/keld-core/src/hello_session.rs` `recovered_crash_baseline`)
- 2026-08-24 [process] `git worktree add` in this repo triggers `competitors-sync`, which clones 11 reference repos (~1.3 GB) into every new worktree, on top of a multi-GB per-worktree `target/`. Four worktrees filled a 228 GB disk to 100%, at which point no tool can write at all — not even a harness output file. Reuse one worktree per concern and delete `competitors/` + `target/` (both gitignored, both regenerable) when done. (evidence: 2026-08-24 session; `du -sh` showed 5 x 1.3G competitors + 5.5G targets)
- 2026-08-24 [runtime] Killing a supervised child does not close its stdout pipe if a grandchild inherited it: the capture-thread join then blocks for the grandchild's whole lifetime (measured: 100us for `sh -c "sleep 5"` which execs, 4.75s for `sh -c "sleep 5 & wait"` which forks). A shell-wrapped test fixture is enough to hit it — `cmd /C "ping ... >NUL"` runs ping as a grandchild. Spawn test children directly unless the shell is the contract. (evidence: KEL-118, CI run 32780024481 failing at 30.007s = the child's lifetime)
- 2026-08-24 [ci] `bunx tsc` with no pin resolves TypeScript 7.x (the native port, 20 platform binaries), not 5.x — a floating major decided at job time. A `packages/*` typecheck lane needs an exact `devDependencies` pin plus a committed `bun.lock` and `bun install --frozen-lockfile`, the same rule as the `oven-sh/setup-bun` 1.4.0 pin. (evidence: KEL-111, `bunx tsc --version` prints 7.0.2 on 2026-08-24)
- 2026-08-25 [ipc] On Windows, `Bun.connect({unix: "\\\\.\\pipe\\<name>"})` (a JS string literal; it evaluates to the path `\\.\pipe\<name>`) connects to a Win32 named pipe — the `unix:` option is the pipe path option too, so KEL-101 needs no second client API and the `process.platform === "win32"` TCP branch collapses into the existing call rather than gaining a third transport shape. `node:net` `createConnection({path})` works identically. Measured on Bun 1.4.0 stable / Windows 11 26200 against a .NET `NamedPipeServerStream` (`PipeOptions.Asynchronous`): full PING/PONG, 3/3 repeats, with a nonexistent-pipe negative control failing. Default pipe security only — this says nothing about the spec's `0x0012_019B` DACL, whose evidence is a separate probe. (evidence: KEL-101, `packages/@keld/electron/src/link.ts:503-513`, `crates/keld-cli/templates/hello/src/kipc.ts:417-427`)
- 2026-08-25 [bench] A Windows working set is not a property of the program: the SAME sha-verified keld.exe running the SAME unchanged fixture under the SAME 30-round protocol on the SAME machine on AC power measured its Bun child at 50,828 KiB one day and 22,968 KiB the next (2.22x), while `runtime_private_kib` moved 0.1% (89,016 vs 88,916) and the within-session spread stayed under 0.2%. Ruled out: binary, fixture, canonical payload (22,972 with vs 22,896 without), and capture time (the child's working set is flat to 0.3% over 20 s). The scored host values reproduced at 1.00x and the paired headline replicated 0.848386 -> 0.847364, so score the process you gate on and treat any working-set SUM as session-local; private bytes is the reproducible counter for the Bun child. (evidence: KEL-25, gyldlab/keld-benches@b30d145 vs a matched 2026-08-26 repeat)
- 2026-08-24 [runtime] `bun --version` can report a version that was never released: this Windows box read `1.4.1` while `bun --revision` showed `1.4.1-canary.1+abe2ad4f0` and the newest published release was `1.4.0`. `bun upgrade --stable` is the fix and prints `Downgrading from Bun 1.4.1-canary to Bun v1.4.0`. This matters because the KEL-77 differential shells out to whatever `bun` is on `PATH` while CI pins `bun-version: "1.4.0"`, so a green local run can be evidence about a runtime CI never tests; always read `bun --revision`, not `--version`, before recording Bun evidence. (evidence: KEL-77, `.github/workflows/ci.yml` bun-version pin, KEL-101 spec DACL probe recorded against "Bun 1.4.1")
- 2026-08-24 [bench/windows] A process-tree RSS sample can be byte-STABLE while the tree is still INCOMPLETE: one run in thirty settled after the titled `keld dev` window was already visible but before any `msedgewebview2` child existed, giving a 55,336 KiB "stable" tree with zero engine processes against a ~372,000 KiB median. A bounded-drift rule alone accepts it. Require an identical process-class census across the drift window plus a non-zero engine floor; adding that cut host-lane stdev 986 -> 69 KiB and engine-lane stdev 59,139 -> 3,376 KiB, so churn had been contaminating many samples, not one. `schema/result.v1.schema.json` already lists "membership churn" as a reject reason. (evidence: keld-benches `windows/bench/Measure-KeldIdleRss.ps1` Wait-BoundedDrift + its negative controls, PR gyldlab/keld-benches#12)
- 2026-08-24 [bench] An evidence document that records its own provenance must DERIVE it, never author it: hardcoding `bench_tree_state: "clean"` and hashing the WORKING-TREE harness shipped three keld-benches documents whose recorded harness sha256 did not exist at the recorded commit (one named a file that was still untracked there). `git show <sha>:<path>` and `git status --porcelain <path>` make both checkable in two lines, and an emitter that refuses to run on a dirty tree catches it before publication — provenance that cannot be checked is worse than absent because it reads as verified. (evidence: keld-benches `windows/bench/Emit-Kel25Documents.ps1` Assert-HarnessAtCommit, PR gyldlab/keld-benches#12 commits bab6c97/abd092d)
- 2026-08-25 [process] CodeRabbit's free tier rate-limits silently: it posts `Review limit reached` instead of a review, leaving the PR with zero unresolved threads and `mergeStateStatus: CLEAN` — indistinguishable from reviewed-and-clean. `@coderabbitai review` then answers "Review triggered" and produces nothing, because incremental review skips already-reviewed commits, so a force-push that *removes* commits can leave a branch permanently unreviewed. Check the commit a review was submitted against (`gh api repos/<o>/<r>/pulls/<n>/reviews`), never the thread count. (evidence: 2026-08-24/25 session; #85/#86/#87/#89 all CLEAN with zero threads while none had been reviewed)
- 2026-08-25 [git] `git checkout -B <branch> origin/main` silently re-points that branch's upstream to `refs/heads/main`, so a later bare `git push --force-with-lease` targets **main**, not the feature branch. Branch protection rejects it, but piping push output through `tail`/`grep` hides the rejection and the branch looks pushed while the remote still holds the old tip. Push feature branches with an explicit refspec (`git push --force-with-lease origin HEAD:refs/heads/<branch>`) and read the full push output. (evidence: 2026-08-25 session, KEL-111/KEL-112 narrowing; `git config --get branch.<name>.merge` returned `refs/heads/main`)
- 2026-08-25 [git] `git checkout -B <branch> origin/main` silently re-points that branch's upstream to `refs/heads/main` (`git config --get branch.<name>.merge` proves it). Under git's default `push.default=simple` a later bare `git push --force-with-lease` then ABORTS client-side ("won't match the local branch, see option 'simple'") — it never reaches the remote, so branch protection is not what saves you and a protected-branch rule would not either; under `push.default=upstream|tracking|matching` the same command lands on `main`. Either way the push output is the only signal, and piping it through `tail`/`grep` hides it while the branch looks pushed and the remote still holds the old tip. Push feature branches with an explicit refspec (`git push --force-with-lease origin HEAD:refs/heads/<branch>`) and read the full output. (evidence: 2026-08-25 session, KEL-111/KEL-112 narrowing; reproduced in a throwaway repo)
- 2026-08-25 [git] `git checkout -B <branch> origin/main` silently re-points that branch's upstream to `refs/heads/main` (`git config --get branch.<name>.merge` proves it). Under git's default `push.default=simple` a later bare `git push --force-with-lease` then ABORTS client-side ("won't match the local branch, see option 'simple'") — it never reaches the remote, so branch protection is not what saves you. Under `push.default=upstream` or `tracking` the same command pushes the current branch to that upstream, i.e. onto `main`; under `matching` it does not follow the upstream at all but pushes every same-named branch, which can update `main` from a local `main` and is a different hazard. Either way the push output is the only signal, and piping it through `tail`/`grep` hides it while the branch looks pushed and the remote still holds the old tip. Push feature branches with an explicit refspec (`git push --force-with-lease origin HEAD:refs/heads/<branch>`) and read the full output. (evidence: 2026-08-25 session, KEL-111/KEL-112 narrowing; reproduced in a throwaway repo)
- 2026-08-27 [git] In a squash-merging repo `git range-diff origin/main...HEAD` cannot match a branch's N commits against the ONE squash commit that landed them, so every commit reports unmatched and a fully-merged branch reads as unmerged work — an audit nearly preserved four worktrees on the strength of "10 unique unmerged commits" that were all already on main. Compare CONTENT, not patch-ids: `B=$(git merge-base origin/main HEAD); git diff origin/main HEAD -- $(git diff --name-only $B..HEAD)` — empty output proves the branch adds nothing main lacks. Also state the baseline when quoting ahead/behind: `origin/main` and the PR's own branch give different numbers for the same worktree (14 vs 16 here) and neither is wrong. (evidence: research note 217 R29/R37, verified across keld-kel105b/112/119/120)
- 2026-08-27 [ci] A required status check that is `if:`-gated reports **skipped**, and GitHub counts skipped as SATISFYING branch protection — so a required check can be green by never running, and the effective merge gate can be empty. `gatekeeper` and `title-lint` are gated `github.event.action != 'synchronize'`, so PR #95 merged with BOTH required checks `skipped` and its `needs-template-fix` label still applied. Read `gh api repos/<o>/<r>/commits/<merged-head>/check-runs`, never the PR summary or the label state. (evidence: research note 217 R22; PR #95 merged head 870612d, run 32980053859)
- 2026-08-27 [docs] Windows PowerShell 5.1 `Get-Content` does not default to UTF-8, so hashing decoded Markdown without `-Encoding utf8` produced a reproducible but wrong approval digest; byte-bound provenance checks must specify the decoder or hash raw bytes. (evidence: KEL-96 decision block; PowerShell and Bun SHA-256 cross-check)
- 2026-08-28 [runtime] A latest-only termination record lets a final exit 0 hide an earlier post-ready crash, while classifying after lease revocation can mislabel a host-caused EOF exit; retain separate crash/all-termination views and classify before revocation. (evidence: KEL-116 isolated reviews, `crates/keld-runtime/src/lib.rs`, `crates/keld-core/src/hello_session.rs`)
- 2026-08-28 [runtime] macOS `proc_setpcontrol(PROC_SETPC_TERMINATE)` controls resource-starvation policy, not parent death; a raw staged host needs an independent guardian whose read end observes EOF from a host-owned non-inheritable liveness writer, after which the guardian signals the enrolled process group and waits the direct Bun child. (evidence: macOS 26.5 SDK `libproc.h`, `pipe(2)`, `killpg(2)`, KEL-78/T2a probe)
- 2026-08-28 [build] Put integration-crate `//!` docs before a crate-level platform `#![cfg(...)]`; on non-target CI the cfg removes following doc attributes and `-D missing-docs` fails the intentionally empty crate. (evidence: KEL-78 PR #108 Windows CI; standalone rustc doc-before-cfg negative control)
- 2026-08-29 [runtime] An anonymous pipe made with `pipe(2)` is inheritable on macOS unless both ends receive `FD_CLOEXEC`; a child-held copy of the host-liveness writer prevents guardian EOF indefinitely, so the production boundary and fixtures must validate/set descriptor isolation before spawn. (evidence: `crates/keld-runtime/src/macos_guardian.rs`; KEL-78/T2b hanging negative observation)
- 2026-08-29 [runtime] A guardian that blocks on host-liveness EOF before observing `Supervisor` terminal events hides Bun self-termination while the host is still live; poll the validated liveness descriptor nonblocking beside supervisor events so either owner can end the session. (evidence: `crates/keld-runtime/src/macos_guardian.rs`; KEL-96 status-zero live-writer test)
- 2026-08-29 [core] A joined blocking stdin lease reader deadlocks normal Quit because `keld dev` must retain its writer while waiting for the host; mark fd 0 nonblocking/CLOEXEC, drain only a fixed budget per guardian-owner poll, and serialize EOF/Call claims plus writes through one transition guard so data cannot starve liveness and exactly one teardown cause wins. (evidence: KEL-96/T2; `crates/keld-core/src/app_session.rs`)
- 2026-08-29 [runtime] A fixed-size record on a timed Unix stream still needs an incremental reader: `read_exact` can consume a prefix and then return `WouldBlock`, so retrying with a fresh buffer corrupts the next record. Retain the filled prefix across poll expiry and test split delivery. (evidence: KEL-96/T3, `macos_guardian::tests::generation_control_reader_retains_split_bytes_across_would_block`)
- 2026-08-29 [runtime] A role principal keyed only by lifecycle category plus a per-coordinator generation collides when multiple `window-bound` declarations exist or a coordinator resets; the app-session owner must retain a stable role-declaration id and monotonic generation state. (evidence: KEL-75/T4a, `crates/keld-runtime/tests/window_bound_contract.rs` `coordinator_recreation_cannot_reset_role_generation`)
- 2026-08-30 [process/macos] macOS SIP strips inherited `DYLD_INSERT_LIBRARIES` when a test launches protected `/bin/sh`; set the variable inside the suspended shell immediately before `exec` of the unsigned staged host, and use a `__DATA,__interpose` entry rather than flat-namespace symbol replacement. (evidence: KEL-102/T2 `PolicyReadFault`, `crates/keld-host/tests/no_flag_macos.rs`)
- 2026-08-30 [ipc/windows] After an authenticated loopback bootstrap consumes its listener, a connect-to-wake cancellation can block while the accepted socket still retains the local port; track listener liveness and skip the accept waker after consumption, leaving authenticated-session shutdown to the bounded reader poll. (evidence: KEL-75/T8, `echo_link::tests::shutdown_interrupts_a_quiet_authenticated_client`)
- 2026-08-30 [cli/windows] A protected child DACL does not pin its launch pathname when a parent grants `FILE_DELETE_CHILD`: another principal can rename the child and recreate attacker bytes before path-based `CreateProcess`. Retain directory handles without `FILE_SHARE_DELETE` across every accepted stage component through host exit, then recheck the locked DACL and executable digest. (evidence: KEL-96/T4 hostile parent replacement probe; `windows_stage_namespace_is_pinned_until_the_host_owner_releases_it`)
- 2026-08-31 [process/windows] After an abnormal host exit, Windows may briefly retain a Job member's current-directory handle after the host process object is signaled. Rust's Windows `remove_dir_all` bounds/yields on child-entry sharing violations but not the final directory's error 32, so an exact validated stage cleanup needs the same narrow whole-operation retry bounded by a monotonic deadline; a fixed yield count exhausted under CI load. Generic retries and sleeps remain forbidden. (evidence: KEL-150 negative control, KEL-150 regression follow-up, GitHub Windows run `33355298740`)
- 2026-08-31 [test/windows] Bun `Socket.flush()` proves only a local flush attempt, not that a Rust peer observed queued control records before `process.exit`; an abrupt exit can yield `WSAECONNRESET` with an empty read. Crash-order fixtures need a peer acknowledgment after the required records, then the intentional exit, so the witness is causal without sleep synchronization. (evidence: KEL-151, GitHub Windows run `33355298740`)
- 2026-08-31 [ci/windows] PowerShell/`just` can launch Git for Windows Bash with `MSYSTEM` unset even though `uname -s` reports `MINGW*`; gating `cygpath` or Docker-bind permission setup only on `MSYSTEM` silently skips both. Detect either signal, and structurally test executable function definitions/calls rather than inert token presence. (evidence: KEL-149 follow-up, CodeRabbit PR #131, real Docker Desktop `EACCES mkdir /out/*-assets` negative observation)
- 2026-08-31 [ipc/windows] Win32 byte-mode named pipes report peer close as `ERROR_BROKEN_PIPE`/`ERROR_NO_DATA`/`ERROR_PIPE_NOT_CONNECTED`, not Rust socket EOF; normalize those read results to zero bytes so framing still owns `KELD-IPC-001` for premature EOF and completed session loops can terminate cleanly. A terminal reply cannot use an unbounded `FlushFileBuffers` worker: write the existing reply, wait under the app-link deadline for the client to close after consuming it, then abortively disconnect; Bun may emit `EPIPE` before `close`, so the client fixture must coalesce both into one link-death notification. (evidence: KEL-101/T3 real Windows no-flag lifecycle and negative controls)
- 2026-09-02 [ci/windows] An unqualified GitHub Actions `run` step on `windows-latest` uses PowerShell; POSIX `mkdir -p` invokes `New-Item` and fails when the Rust cache already restored the directory. Set `shell: bash` for POSIX blocks and contract-test the shell, unique step, and exact condition. (evidence: CodeRabbit PR #136, GHA 33622370474, `tools/ci_hygiene.rs`)
- 2026-09-04 [ci/linux] Ubuntu 24.04 AppArmor may deny Bubblewrap loopback setup; mirror upstream's userns CI sysctl, never retry or skip. (evidence: PR #146, bubblewrap `f97804f`)
