[package]
name = "keld-core"
description = "Keld host runtime: lifecycle session + hello window (destination: event loop integration, window registry, plugin host)"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[dependencies]
keld-ipc = { workspace = true }
keld-guard = { workspace = true }
keld-runtime = { workspace = true }
keld-wv = { workspace = true }

[lints]
workspace = true
